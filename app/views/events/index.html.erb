
<a href="/"><h1 class="logo">ShareMyWay</h1></a>

<div class="container event-index">
<%#  si dessous la search bar pour rentrer une localisation %>
  <div class="searchbar">
    <div>
      <%= form_with url: events_path, method: :get, class: "d-flex" do %>
        <%= text_field_tag :query,
              params[:query],
              class: "form-control",
              placeholder: "Search event location"
          %>
        <%# <%= submit_tag "Search", address: "", class: "btn btn-primary m-1" %>
      <% end %>
    </div>
  </div>

  <div class="cards content-event">
    <div class="events-list" data-controller="events-list">
      <% @events.each do |event| %>
        <%= link_to event_path(event) do %>
          <div class="card-event">
            <%= cl_image_tag(event.photo.key, alt: "NA", class: "card-img-top image") %>
              <%# La ligne si dessous est codée pour ne pas oublié de créer un btn qui nous permet d'afficher les membres participants à l'event %>
              <%# <%= link_to 'More', members_path(event), class: 'btn btn-primary' %>
              <div class="event-infos">
                <div class="event-title"><%= event.name %></div>
                <div class="location-wrapper d-flex align-items-center">
                  <i class="fa-solid fa-location-pin icon-info where"></i>
                  <div class="location mb-2"><%= event.address %></div>
                </div>
                <div class="date-wrapper d-flex align-items-center">
                  <i class="fa-regular fa-calendar icon-info when"></i>
                  <div class="display-date"><%= event.date %></div>
                </div>
                <%= cl_image_tag(event.user.photo.key, alt: "NA", class: "card-trip-user avatar-bordered") %>
                <%# <div class="icone-categories">
                  <i class="fa-solid fa-campground"></i>
                </div> %>
                <div class="icone-categories">
                  <% if event.category == "Hiking and Trekking" %>
                    <i class="fa-solid fa-person-hiking"></i>
                  <% elsif event.category == "Backpacking and Camping" %>
                    <i class="fa-solid fa-campground"></i>
                  <% elsif event.category == "Road Trips and Drives" %>
                    <i class="fa-solid fa-car"></i>
                 <% elsif event.category == "Wildlife and Safari" %>
                    <i class="fa-solid fa-hippo"></i>
                  <% elsif event.category == "Language Learning" %>
                    <i class="fa-solid fa-language"></i>
                  <% elsif event.category == "Photography Workshops" %>
                    <i class="fa-solid fa-camera"></i>
                  <% elsif event.category == "Cooking Classes" %>
                    <i class="fa-solid fa-cookie-bite"></i>
                  <% elsif event.category == "Coding and Tech Workshops" %>
                    <i class="fa-solid fa-book-open"></i>
                  <% elsif event.category == "Yoga and Meditation" %>
                    <i class="fa-solid fa-spa"></i>
                  <% elsif event.category == "Group Fitness Classes" %>
                    <i class="fa-solid fa-people-group"></i>
                  <% elsif event.category == "Outdoor Sports" %>
                    <i class="fa-solid fa-cloud-sun"></i>
                  <% else event.category == "Personal Training" %>
                    <i class="fa-solid fa-dumbbell"></i>
                  <% end %>
                </div>
              </div>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
